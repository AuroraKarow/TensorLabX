#pragma once

#ifndef __NET_CHRONO__
#define __NET_CHRONO__

#include "net_list"

#define NEUNET_CHRONO_TIME_POINT    std::chrono::time_point_cast<std::chrono::milliseconds>(std::chrono::system_clock::now()).time_since_epoch().count()

#define NEUNET_CHRONO_BEGIN         neunet::glb_timer.timer_begin()
#define NEUNET_CHRONO_END(ID)       neunet::glb_timer.timer_end(ID)
#define NEUNET_CHRONO_DURATION(ID)  neunet::glb_timer.duration(ID)

#include "net_chrono.hpp"

#endif